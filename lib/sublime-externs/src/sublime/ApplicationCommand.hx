
package sublime;

class ApplicationCommand {

	//public function run(<args>)	None	Called when the command is run.
	//public function is_enabled(<args>)	bool	Returns true if the command is able to be run at this time. The default implementation simply always returns True.
	//public function is_visible(<args>)	bool	Returns true if the command should be shown in the menu at this time. The default implementation always returns True.
	//public function is_checked(<args>)	bool	Returns true if a checkbox should be shown next to the menu item. The .sublime-menu file must have the checkbox attribute set to true for this to be used.
	//public function description(<args>)	String	Returns a description of the command with the given arguments. Used in the menu, if no caption is provided. Return None to get the default description.

}